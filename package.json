{
  "name": "ai-token-monitor",
  "version": "1.0.0",
  "description": "Real-Time Multi-Brand AI Token Monitor Dashboard",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "check": "node --check server.js app.js && for f in lib/*.js; do node --check \"$f\"; done",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  },
  "keywords": [
    "ai",
    "token",
    "monitor",
    "dashboard",
    "real-time"
  ],
  "author": "Antigravity",
  "license": "ISC",
  "dependencies": {
    "@google/generative-ai": "^0.24.1"
  }
}
